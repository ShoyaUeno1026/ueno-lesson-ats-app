
<div class="row justify-content-center">
  <div class="col-lg-8">
    <h1 class="h2"><%= t(".title") %></h1>

    <%= render "form", match: @match %>

    <div>
      <%= link_to t("back"), account_job_path(@account, @match.job) %>
    </div>
  </div>
</div>
