
<div class="row justify-content-center">
  <div class="col-lg-8">
    <h1 class="h2"><%= t(".title") %></h1>

    <%= render "form", department: @department %>

    <div>
      <%= link_to t("detail"), [@account, @department] %>
      <%= link_to t("back"), account_departments_path(@account) %>
    </div>
  </div>
</div>
